import type {
  EventKey,
  KeyEventDetails,
  KeyEventModifiers,
  Maybe,
  Options,
} from './types.js'

import { event2string, details } from './functions.js'

export type { EventKey, KeyEventDetails, KeyEventModifiers, Maybe, Options }

export { details, event2string }
